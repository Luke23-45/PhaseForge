"""Executable data scripts (download, prep, verify).

Run as modules, e.g.::

    python -m phaseforge.data.scripts.<script> --help

NOTE: the former benchmark-specific dataset scripts are archived under
``legacy/``. New ingestion is source/ingester based and does not download
data.
"""
