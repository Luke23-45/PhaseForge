"""F1 — method overview schematic (hand-drawn; no generated data).

The registry entry exists so ``verify`` expects the file; draw it in any
vector editor and place it at ``figures/main/F1_overview.pdf``.
"""

from __future__ import annotations

MESSAGE = (
    "F1 is a hand-drawn schematic. Create it in a vector editor and export to "
    "figures/main/F1_overview.pdf (paper_root-relative)."
)
